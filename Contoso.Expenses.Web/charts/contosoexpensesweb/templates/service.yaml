apiVersion: v1
kind: Service
metadata:
  name: {{ template "contosoexpensesweb.fullname" . }}
  labels:
    app: {{ template "contosoexpensesweb.name" . }}
    chart: {{ template "contosoexpensesweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "contosoexpensesweb.name" . }}
    release: {{ .Release.Name }}
